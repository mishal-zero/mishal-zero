    ###- this is an out of band vulnerability testing tool created with python and tkinter ui. named --killtheparser--
    ***- usage
    #- run python3 ktp.py
    #- select url list from the top panel. (url list should be obatined through crawling the web)
    #- setup callback server with burp-collabarator or interact.sh
    #- select payload list or type payloads on the given panel (blind-payload with specified callback url)
    #- click start...
    listen through callback server...
